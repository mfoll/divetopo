# Previous good renders

This directory preserves the 21 published JPEG outputs from commit `66ad173`
for direct visual comparison with the regenerated files in `outputs/`.

These images are reference snapshots only. The current canonical renders remain
in the parent directory.
